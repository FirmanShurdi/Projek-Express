<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/index.css" />
  </head>
  <body>
    <%- include('partials/navbar') %> <% if (success) { %>
    <div class="success"><%= success %></div>
    <% } %>
    <div class="marquee-container">
      <div class="marquee-text">
        Welcome to Madura Pride, tempat para king mulet, dan jamet bersatu padu
        menjadi satu !
      </div>
    </div>
    <section class="hero-section">
      <div class="hero-content">
        <small>Selamat datang di Meduro</small>
        <h1>Selamat Datang di Meduro Pride</h1>
        <p>
          Selama lebih dari 90 generasi, kami percaya bahwa
          <strong>manusia mulet dan jamet</strong> dapat hidup berbahagia
          bersama. Mari kita mulai petualanganmu!
        </p>
        <div class="cta-buttons">
          <a href="/objek?kategori=hewan" class="btn btn-primary">Hewan</a>
          <a href="/objek?kategori=buah" class="btn btn-secondary">Buah</a>
        </div>
      </div>
      <div class="hero-image">
        <img src="/images/utama/madura.jpg" alt="Hewan Bahagia" />
      </div>
    </section>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <!-- penghilang msg -->
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const alertBox = document.querySelector(".success");
        if (alertBox) {
          setTimeout(() => {
            alertBox.style.opacity = "0";
            setTimeout(() => alertBox.remove(), 500);
          }, 3000);
        }
      });
    </script>
    <script>
      window.addEventListener("DOMContentLoaded", () => {
        const section = document.querySelector(".hero-section");
        section.style.opacity = 0;
        section.style.transform = "translateY(40px)";
        setTimeout(() => {
          section.style.transition = "all 0.8s ease";
          section.style.opacity = 1;
          section.style.transform = "translateY(0)";
        }, 300);
      });
    </script>

    <%- include('partials/footer') %>
  </body>
</html>
