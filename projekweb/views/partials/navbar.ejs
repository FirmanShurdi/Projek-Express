<div class="navbar">
  <div class="logo">
    <a href="/">
      <img src="/images/utama/logo1.jpg" alt="Meduro Logo" />
    </a>
    <span class="brand-name">
      <span class="meduro">Meduro</span><span class="pride">Pride</span>
    </span>
  </div>

  <button class="menu-toggle" onclick="toggleMenu()">☰</button>

  <div class="nav-links">
    <a href="/">Home</a>
    <a href="/objek?kategori=hewan">Hewan</a>
    <a href="/objek?kategori=buah">Buah</a>
    <% if (session && session.role === 'admin') { %>
      <a href="/master">Master</a>
    <% } %>

    <form action="/search" method="GET" class="search-form">
      <input type="text" name="q" placeholder="Search..." required />
      <button type="submit">
        <img src="https://cdn-icons-png.flaticon.com/512/622/622669.png" alt="Search" />
      </button>
    </form>
  </div>


  <% if (session && session.isLoggedIn) { %>
    <div class="auth-section">
      <span class="username">Hi, <%= session.username %></span>
      <a href="/logout" class="logout-btn">Logout <img src="https://cdn-icons-png.flaticon.com/512/1828/1828479.png" alt="Logout Icon" /></a>
    </div>
  <% } else { %>
    <div class="login-link">
      <a href="/login">Login ⏎</a>
    </div>
  <% } %>
</div>
<script>
  function toggleMenu() {
    const nav = document.querySelector('.nav-links');
    nav.classList.toggle('active');
  }
</script>
<style>
/* WARNA PALET */
:root {
  --dark-green: #1e352f;
  --primary-green: #3b7d58;
  --light-green: #a8dadc;
  --accent-green: #5cb38b;
  --text-light: #f1fdf4;
  --text-muted: #cce3d4;
}

/* NAVBAR STYLES */
.navbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background-color: var(--dark-green);
  padding: 14px 28px;
  flex-wrap: wrap;
  font-family: 'Segoe UI', sans-serif;
  color: var(--text-light);
  box-shadow: 0 4px 10px rgba(30, 53, 47, 0.5);
  border-bottom: 3px solid var(--primary-green);
  position: relative;
  z-index: 99;
}

.logo {
  display: flex;
  align-items: center;
  gap: 10px;
}

.logo img {
  width: 46px;
}

.brand-name {
  font-size: 27px;
  font-weight: bold;
  letter-spacing: 0.5px;
}

.brand-name .meduro {
  color: var(--light-green);
  font-family:'Cinzel', serif;
}

.brand-name .pride {
  color: var(--text-light);
   font-family:'Cinzel', serif;
}

.menu-toggle {
  display: none;
  background: none;
  font-size: 24px;
  color: var(--light-green);
  border: none;
  cursor: pointer;
  margin-left: auto;
}

.nav-links {
  display: flex;
  align-items: center;
  gap: 20px;
  flex-wrap: wrap;
  margin-right: 10px;
}

.logout-btn {
    color: var(--text-light);
    text-decoration: none;
    background-color: var(--primary-green);
    padding: 10px 14px;
    transition: 0.3s;
    display: flex;
    align-items: center;
    border-radius: 5px;
    gap: 8px;
    
}
.nav-links a,
.login-link a {
  color: var(--text-light);
  text-decoration: none;
  padding: 8px 14px;
  border-radius: 6px;
  background-color: var(--primary-green);
  transition: background-color 0.3s ease, transform 0.2s;
}

.nav-links a:hover,
.logout-btn:hover,
.login-link a:hover {
  background-color: var(--accent-green);
  transform: scale(1.05);
}

  .search-form {
    display: flex;
    align-items: center;
    background-color: #f5f5f5;
    border-radius: 999px;
    padding: 8px 12px;
    gap: 10px;

  }

  .search-form input {
    border: none;
    background: transparent;
    outline: none;
    font-size: 16px;
    color: #333;
    flex: 1;
  }

  .search-form input::placeholder {
    color: #aaa;
  }

  .search-form button {
    background-color: #28a7f5;
    border: none;
    border-radius: 50%;
    width: 25px;
    height: 22px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }

  .search-form button:hover {
    background-color: #1a92db;
  }

  .search-form button img {
    width: 18px;
    height: 18px;
  }


.auth-section {
  display: flex;
  align-items: center;
  gap: 8px;
}
  .login-link img,
  .logout-btn img {
    width: 20px;
    height: 20px;
  }
.username {
  color: var(--text-muted);
    font-weight: bold;
}

@media (max-width: 769px) {
    .menu-toggle {
      display: block;
    }
    .logo img{
      margin-left: 115px;

    }
    .nav-links {
      flex-direction: column;
      width: 100%;
      display: none;
      margin-right: 30px;
      margin-top: 10px;

    }

    .nav-links.active {
      display: flex;
    }

    .nav-links a,
    .search-form,
    .login-link a,
    .logout-btn {
      width: 100%;
      justify-content: center;
      text-align: center;;
    }

    .search-form {
      margin-left: 0;
      margin-top: 5px;
      margin-bottom:5px
    }

    .login-link {
      width: 100%;
      justify-content: flex-end;
      margin-top: 10px;
      margin-left: 400px;
    }
    .auth-section,
    .logout-btn {
      width: 100%;
      margin-top: 10px;
    }

    .search-form input {
      width: 70%;
    }

    .search-form button {
      width: 32px;
      height: 32px;
    }
  }
  

</style>

